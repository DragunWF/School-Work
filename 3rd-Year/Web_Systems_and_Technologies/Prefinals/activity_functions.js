// 06 ELMS Activity 1 - Marc Plarisan | BSIT601

// 1.
function printName(name) {
  console.log(`My name is ${name}.`);
}

// 2.
function addTwoNumbers(firstNumber, secondNumber) {
  const sum = firstNumber + secondNumber;
  console.log(sum);
  return sum;
}

// 3.
function addThreeNumbers(firstNumber, secondNumber, thirdNumber) {
  const sum = firstNumber + secondNumber + thirdNumber;
  console.log(sum);
  return sum;
}

printName("Marc Plarisan");
addTwoNumbers(5, 3);
addThreeNumbers(12, 13, 17);
